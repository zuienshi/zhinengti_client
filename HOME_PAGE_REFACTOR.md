# Home 页面千川风格改造说明

## 改造时间
2025-10-23

## 改造目标

将 Home.vue 页面从暗黑模式改造为类似抖音千川后台的浅色风格，同时主窗口启用系统原生标题栏，支持窗口大小调整。

## 主要改动

### 1. 主窗口配置调整 (main.js)

#### 修改前
```javascript
mainWindow = new BrowserWindow({
  width: 1050,          // 固定宽度
  height: 700,          // 固定高度
  frame: false,         // 无边框
  resizable: false,     // 禁止调整大小
  maximizable: false,   // 禁止最大化
  fullscreenable: false // 禁止全屏
})
```

#### 修改后
```javascript
mainWindow = new BrowserWindow({
  width: 1200,              // 默认宽度（可调整）
  height: 800,              // 默认高度（可调整）
  minWidth: 1000,           // 最小宽度限制
  minHeight: 600,           // 最小高度限制
  frame: true,              // ✅ 系统标题栏（包含 -□× 按钮）
  title: '智能体应用',      // 标题栏文本
  resizable: true,          // ✅ 允许调整大小
  maximizable: true,        // ✅ 允许最大化
  fullscreenable: true,     // ✅ 允许全屏
  autoHideMenuBar: true,    // ✅ 隐藏菜单栏（File、Edit 等）
  center: true              // 窗口居中
})
```

**关键改进：**
- 使用 Windows 原生标题栏，用户体验更好
- 允许用户自由调整窗口大小
- 自动隐藏系统菜单栏，界面更简洁
- 设置最小尺寸限制，避免窗口过小

### 2. Home.vue 样式全面改造

#### 2.1 色彩方案转换

**暗黑模式 → 千川浅色风格**

| 元素 | 暗黑模式 | 千川风格 | 说明 |
|------|---------|---------|------|
| 页面背景 | #0a0a0a | #f0f2f5 | 浅灰色背景 |
| 侧边栏背景 | #000000 | #001529 | 深蓝色 |
| 侧边栏Logo区 | #000000 | #002140 | 更深的蓝 |
| 顶部导航背景 | #121212 | #ffffff | 白色 |
| 主内容区背景 | #1a1a1a | #f0f2f5 | 浅灰色 |
| 主文本颜色 | #ffffff | rgba(0,0,0,0.85) | 深色文本 |
| 主题色 | #45ff93 | #1890ff | 蓝色系 |

#### 2.2 侧边栏样式调整

**修改前（暗黑风格）：**
- 宽度：240px
- 背景：纯黑色 #000000
- 菜单项：深绿色背景，绿色高亮
- 激活状态：绿色阴影效果

**修改后（千川风格）：**
- 宽度：200px（更紧凑）
- 背景：深蓝色 #001529（经典导航色）
- 菜单项：半透明白色悬停效果
- 激活状态：蓝色背景 #1890ff + 左侧白色竖条

```css
/* 侧边栏 */
.sidebar-container {
  width: 200px;
  background-color: #001529; /* 深蓝色 */
  box-shadow: 2px 0 8px 0 rgba(29, 35, 41, 0.05);
}

/* Logo 区域 */
.logo-container {
  height: 64px;
  background-color: #002140; /* 更深的蓝 */
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

/* 菜单项 */
.menu-item {
  color: rgba(255, 255, 255, 0.85);
  height: 48px;
  padding: 0 16px 0 24px;
}

.menu-item:hover {
  background-color: rgba(255, 255, 255, 0.08);
}

.menu-item.active {
  background-color: #1890ff; /* 蓝色高亮 */
  position: relative;
}

.menu-item.active::before {
  content: '';
  position: absolute;
  left: 0;
  width: 3px;
  background-color: #fff; /* 左侧白色竖条 */
}
```

#### 2.3 顶部导航栏样式

**修改前：**
- 背景：深色 #121212
- 标题：绿色 #45ff93
- 高度：60px

**修改后：**
- 背景：白色 #ffffff
- 标题：深色 rgba(0,0,0,0.85)
- 高度：64px（标准化）
- 阴影：0 1px 4px 0 rgba(0,21,41,0.12)

```css
.header-container {
  height: 64px;
  background-color: #fff;
  box-shadow: 0 1px 4px 0 rgba(0, 21, 41, 0.12);
  padding: 0 24px;
}

.header-title {
  font-size: 18px;
  font-weight: 500;
  color: rgba(0, 0, 0, 0.85);
}
```

#### 2.4 主内容区域

**修改前：**
- 背景：深灰色 #1a1a1a
- 文本：白色
- 卡片：深色背景，绿色边框高亮

**修改后：**
- 背景：浅灰色 #f0f2f5
- 文本：深色
- 卡片：白色背景（由子页面定义）

```css
.main-container {
  flex: 1;
  background-color: #f0f2f5; /* 浅灰背景 */
  padding: 24px;
  overflow-y: auto;
}
```

### 3. App.vue 全局样式调整

**修改前：**
```css
body {
  background-color: var(--bg-primary, #121212);
  color: var(--text-primary, #e0e0e0);
}
```

**修改后：**
```css
body {
  background-color: #f0f2f5; /* 浅灰色背景 */
  color: rgba(0, 0, 0, 0.85); /* 深色文本 */
}
```

### 4. 响应式设计优化

#### 新增响应式断点

```css
/* 中等屏幕 - 侧边栏收起 */
@media (max-width: 1200px) {
  .sidebar-container {
    width: 64px; /* 仅显示图标 */
  }
  
  .menu-item span,
  .submenu-item span {
    display: none; /* 隐藏文字 */
  }
}

/* 小屏幕 - 内边距调整 */
@media (max-width: 768px) {
  .main-container {
    padding: 16px;
  }
}
```

## 功能保持不变

以下功能在改造后完全保持：

1. ✅ **退出登录功能** - 完整的五步流程
2. ✅ **菜单导航** - 子菜单展开/收起
3. ✅ **路由跳转** - 直播列表、添加主播、AI审核
4. ✅ **用户下拉菜单** - 个人设置、退出登录
5. ✅ **登录状态检查** - 未登录自动跳转

## 窗口对比

### 登录窗口（保持不变）
- 尺寸：450×700 固定
- 标题栏：无边框（自定义）
- 样式：保持原有设计

### 主窗口（已改造）
- 尺寸：1200×800 默认，可调整
- 最小尺寸：1000×600
- 标题栏：**Windows 原生标题栏**
- 按钮：系统的 - □ ×
- 菜单栏：自动隐藏（按 Alt 显示）

## 抖音千川风格特点

### 已实现的设计元素

1. **左侧深色导航**
   - 深蓝色背景 #001529
   - 白色图标和文字
   - 蓝色高亮选中状态
   - 左侧白色竖条指示器

2. **浅色主题**
   - 页面背景：#f0f2f5
   - 卡片背景：白色
   - 柔和的阴影效果

3. **现代化布局**
   - 顶部 64px 导航栏
   - 左侧 200px 侧边栏（可收起到 64px）
   - 主内容区 24px 内边距

4. **蓝色主题色**
   - 主题色：#1890ff
   - 悬停：rgba(255,255,255,0.08)
   - 一致的交互反馈

## 测试清单

### 窗口功能测试

- [x] 窗口可以调整大小
- [x] 窗口可以最大化
- [x] 窗口可以最小化
- [x] 窗口可以关闭
- [x] 最小尺寸限制有效（1000×600）
- [x] 菜单栏默认隐藏
- [x] 按 Alt 可以显示菜单栏

### 界面功能测试

- [x] 侧边栏菜单正常显示
- [x] 菜单项点击可展开/收起
- [x] 子菜单项可以选中
- [x] 路由跳转正常
- [x] 顶部用户菜单正常
- [x] 退出登录功能正常
- [x] 登录状态检查正常

### 响应式测试

- [ ] 窗口缩小到 1200px 以下，侧边栏收起
- [ ] 窗口缩小到 768px 以下，内边距调整
- [ ] 内容区域正常滚动

## 相关文件

### 已修改的文件
1. **main.js** - 主窗口配置
   - 修改窗口尺寸设置
   - 启用系统标题栏
   - 添加自动隐藏菜单栏

2. **src/views/Home.vue** - 主页面样式
   - 完全重写 CSS 样式
   - 从暗黑模式改为浅色主题
   - 应用千川风格色彩方案

3. **src/App.vue** - 全局样式
   - 移除暗黑模式背景色
   - 应用浅色主题

### 未修改的文件
- src/views/Login.vue - 登录页面保持不变
- src/router/index.js - 路由配置保持不变
- src/store/index.js - 状态管理保持不变

## 视觉效果对比

### 改造前（暗黑模式）
```
┌─────────────────────────────────────┐
│ 自定义标题栏 (黑色)    - □ ×       │
├─────────────────────────────────────┤
│ ┌────┬──────────────────────────┐   │
│ │黑色│  深灰色顶栏              │   │
│ │侧边│  绿色标题                │   │
│ │栏  ├──────────────────────────┤   │
│ │绿色│  深灰色内容区             │   │
│ │高亮│  白色文本                │   │
│ └────┴──────────────────────────┘   │
└─────────────────────────────────────┘
```

### 改造后（千川风格）
```
┌─────────────────────────────────────┐
│ 智能体应用         [系统按钮 -□×]   │ ← Windows 原生
├─────────────────────────────────────┤
│ ┌────┬──────────────────────────┐   │
│ │深蓝│  白色顶栏                │   │
│ │侧边│  黑色标题                │   │
│ │栏  ├──────────────────────────┤   │
│ │蓝色│  浅灰色内容区             │   │
│ │高亮│  深色文本                │   │
│ └────┴──────────────────────────┘   │
└─────────────────────────────────────┘
```

## 下一步建议

### 可选的进一步优化

1. **数据概览页面**
   - 创建 Dashboard.vue 作为首页
   - 显示数据统计卡片
   - 添加图表展示

2. **面包屑导航**
   - 在顶部导航栏添加面包屑
   - 显示当前路径

3. **侧边栏收起功能**
   - 添加收起/展开按钮
   - 仅显示图标模式

4. **主题色定制**
   - 支持主题色切换
   - 保存用户偏好

5. **动画效果**
   - 页面切换动画
   - 菜单展开动画
   - 卡片加载动画

## 总结

本次改造成功将 Home 页面从暗黑模式转换为类似抖音千川的现代浅色风格，主要特点：

✅ **视觉效果**
- 清爽的浅色主题
- 专业的蓝色配色
- 现代化的扁平设计

✅ **用户体验**
- 系统原生标题栏
- 窗口可自由调整
- 响应式布局

✅ **功能完整**
- 所有原有功能保持
- 退出登录流程完整
- 路由导航正常

---

**改造完成时间：** 2025-10-23  
**改造版本：** v2.0.0  
**改造人：** AI Assistant
